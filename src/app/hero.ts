let nextId = 1;
let nexId = 1;

export class Hero {
  id: number;
  constructor(
    public name: string,
    public power?: string) {
      this.id = nextId++;
      alert(this.id);
    alert(this.id);
    alert(this.id);
    }
}
